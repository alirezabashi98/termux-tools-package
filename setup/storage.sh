#!/bin/bash

#Mobile App can only access its own data by default.
# If you want to access the storage of your mobile phone, you need to request permission.
# If you just accidentally refused, you can execute the following commands to regain access:
termux-setup-storage